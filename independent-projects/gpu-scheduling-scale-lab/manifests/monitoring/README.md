# Monitoring manifests

This phase records Kubernetes API state and the kind control-plane container's Docker statistics. No externally exposed Prometheus service is installed. Future local monitoring must bind port-forwards to `127.0.0.1`.
