# Examples

Use the tracked profiles in `experiments/profiles/` as safe starting points. Override non-secret values such as `BATCH_SIZE` or `WAIT_TIMEOUT_SECONDS` in the invoking shell; do not create secret-bearing profile files.
